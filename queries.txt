create schema project;

CREATE TABLE `PROJECT`.`PARENT_TASK` ( 
PARENT_ID int NOT NULL AUTO_INCREMENT, 
PARENT_TASK_DESC VARCHAR(50) DEFAULT NULL,
PRIMARY KEY (PARENT_ID) 
);

insert into PROJECT.PARENT_TASK(PARENT_TASK_DESC) values ("Parent Task 2");

#drop table PROJECT.PARENT_TASK;
COMMIT;
SELECT * FROM PROJECT.PARENT_TASK;

CREATE TABLE `PROJECT`.`PROJECTS` ( 
PROJECT_ID INT NOT NULL AUTO_INCREMENT, 
PROJECT_DESC VARCHAR(50) DEFAULT NULL,
START_DATE DATE DEFAULT NULL,
END_DATE DATE DEFAULT NULL,
PRIORTY INT DEFAULT 0,
PRIMARY KEY (PROJECT_ID) 
);

INSERT INTO PROJECT.PROJECTS(PROJECT_DESC, START_DATE, END_DATE, PRIORTY)
VALUES ("Some project", "2017-06-08", "2017-06-08", 1);

#drop table PROJECT.PROJECTS;
COMMIT;
SELECT * FROM PROJECT.PROJECTS;


CREATE TABLE `PROJECT`.`TASK` ( 
TASK_ID INT NOT NULL AUTO_INCREMENT, 
PARENT_ID INT  DEFAULT NULL,
PROJECT_ID INT  DEFAULT NULL,
TASK_DESC VARCHAR(50) DEFAULT NULL,
START_DATE DATE DEFAULT NULL,
END_DATE DATE DEFAULT NULL,
PRIORTY INT DEFAULT 0,
TASK_STATUS BOOLEAN DEFAULT FALSE,
PRIMARY KEY (TASK_ID) 
);

drop table PROJECT.TASK;
commit;
SELECT * FROM PROJECT.TASK;

CREATE TABLE `PROJECT`.`USERS` ( 
USER_ID INT NOT NULL AUTO_INCREMENT, 
FIRST_NAME VARCHAR(50) DEFAULT NULL,
LAST_NAME VARCHAR(50) DEFAULT NULL,
EMP_ID INT NOT NULL, 
PROJECT_ID INT  DEFAULT NULL,
TASK_ID INT  DEFAULT NULL,
PRIMARY KEY (USER_ID) 
);


#drop table PROJECT.USERS;
commit;
SELECT * FROM PROJECT.USERS;
UPDATE PROJECT.USERS SET FIRST_NAME="Ayan", LAST_NAME="Sengupta", 
EMP_ID=1, PROJECT_ID=1, TASK_ID=1 
WHERE USER_ID=1;

insert into PROJECT.USERS(FIRST_NAME, LAST_NAME, EMP_ID, PROJECT_ID, TASK_ID)
values ("James", "Dow", 2, 2, 2);
